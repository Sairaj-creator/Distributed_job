taskflow.workerThreads=4
taskflow.httpPort=8081

# Security configuration (Required for Production)
# taskflow.api.key=
# taskflow.cors.allowlist=
taskflow.db.url=jdbc:h2:mem:taskflow;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
taskflow.db.user=sa
taskflow.db.password=
taskflow.profile=dev
